# 进度同步（2026-04-21）

## 今日已完成

### 1. 工程从零搭建（按 `cursor.md`）
- **Vite + Vanilla JS** 工程初始化
- 集成 **Cesium.js** 与 **`vite-plugin-cesium`**
- `build` 后确保 Cesium 资源在 `dist/` 根目录包含：
  - `Assets/`
  - `Workers/`
  - `Widgets/`
  - 同时保留插件默认输出的 `dist/cesium/`（用于兼容）

### 2. 去云化底座初始化
- `Cesium.Ion.defaultAccessToken = null`
- Viewer 默认 UI 控件关闭（geocoder/homeButton/timeline/animation 等）
- 不加载 Cesium 默认底图：`imageryProvider: false` + `imageryLayers.removeAll()`
- 初始视角：**北京正上空**（约 2000km，高俯视）
- 隐藏 Cesium Credits（水印/署名区域）：`viewer.cesiumWidget.creditContainer.style.display = "none"`

### 3. 底图切换
- 天地图 WMTS（token 通过 `.env.local` 的 `VITE_TIANDITU_TOKEN` 配置）
- 高德底图（GCJ-02→WGS84 纠偏）
  - 纠偏：`src/modules/tilingSchemeGcj02.js` + `src/modules/transformGcj02.js`
  - 解决不出图：Vite 反代 `/gaode` → `http://wprd01.is.autonavi.com`（避免跨域与 404）
- 本地离线瓦片：`public/tiles/{z}/{x}/{y}.png`

### 4. 漫游（巡回）
- 漫游下拉：故宫 / 长城 / 上海
- 点击“漫游”：到目标点上方 **20km** 做一圈自动环绕，结束后返回初始点
- 新增“取消”：立即终止飞行/环绕并返回初始点

### 5. 专题图（SingleTileImageryProvider）
- 支持叠加一张带定位信息的专题图图片：
  - 图片：`public/map/tif2.png`
  - 世界文件：`public/map/中国_省/tif2.pgw`
- 自动读取图片像素尺寸 + 解析 `.pgw` 计算四至范围，生成 `Cesium.Rectangle` 后叠加
- 兼容 Cesium 新版：显式传入 `tileWidth/tileHeight`
- “清空专题图”：
  - 移除叠加层
  - **自动回到初始点**

### 6. 图例与比例尺
- 叠加 tif 后右下角显示图例：
  - 竖向色带（参考 ArcGIS 蓝→绿拉伸视觉）
  - 仅显示 `Low` / `High`，不显示具体数值
- 左下角常态比例尺：
  - 随视角/缩放实时更新（m / km）
  - 端点/中点刻度 + 分段填充样式（更接近专业 GIS 控件）

### 7. UI 统一调整
- 顶部工具条、按钮、下拉、图例、比例尺统一为更硬朗的深色专业风格

## 关键文件位置速记
- **入口**：`src/main.js`
- **底图管理**：`src/modules/mapControl.js`
- **高德纠偏**：`src/modules/tilingSchemeGcj02.js`、`src/modules/transformGcj02.js`
- **相机漫游/取消**：`src/modules/cameraControl.js`
- **专题图（SingleTile）**：`src/modules/thematicLayer.js`
- **比例尺**：`src/modules/scaleBar.js`
- **样式**：`src/style.css`
- **Vite 代理（高德）**：`vite.config.js`

## 未完成 / 下次计划

### 本地化部署（去外网依赖）
- 天地图/高德属于外部服务：需要做“纯局域网可用”的本地化替代方案
  - 方案：改为使用离线瓦片 / 局域网 WMTS / 自建瓦片服务
  - Nginx 部署配置与资源路径校验

